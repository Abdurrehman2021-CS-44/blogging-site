<%- include("partials/header") -%>

    <h1>Home</h1>
    <p><%= startingContent %></p>
    <% allPosts.forEach(function(post){ %>
        <h1><%= post.title %></h1>
        <% var link = "/posts/" + post.title %>
        <p><%= post.body.substr(0, 100) + " ..." %> <a href=<%= link.replace(/ /g, '-'); %>>Read More</a> </p>
    <% }); %>
    <% console.log(allPosts) %>

<%- include("partials/footer") -%>